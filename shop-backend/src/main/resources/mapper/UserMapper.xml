<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.UserMapper">

    <resultMap id="UserResultMap" type="User">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="points"    column="points"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <select id="findByPhone" resultMap="UserResultMap">
        SELECT * FROM `user` WHERE phone = #{phone}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM `user` ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (phone, password, nickname, created_at)
        VALUES (#{phone}, #{password}, #{nickname}, NOW())
    </insert>

    <update id="update" parameterType="User">
        UPDATE `user`
        <set>
            <if test="phone != null">phone = #{phone},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM `user` WHERE id = #{id}
    </delete>

    <select id="getPoints" resultType="int">
        SELECT COALESCE(points, 0) FROM `user` WHERE id = #{userId}
    </select>

    <!-- COALESCE 处理历史用户 points=NULL 的情况 -->
    <update id="addPoints">
        UPDATE `user` SET points = COALESCE(points, 0) + #{delta} WHERE id = #{userId}
    </update>

    <!-- AND points >= #{delta} 防超扣，返回 0 行表示余额不足 -->
    <update id="deductPoints">
        UPDATE `user` SET points = points - #{delta}
        WHERE id = #{userId} AND COALESCE(points, 0) >= #{delta}
    </update>

</mapper>
