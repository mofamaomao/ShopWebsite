<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.PointsRecordMapper">

    <resultMap id="PointsResultMap" type="com.shop.entity.PointsRecord">
        <id     property="id"        column="id"/>
        <result property="userId"    column="user_id"/>
        <result property="type"      column="type"/>
        <result property="points"    column="points"/>
        <result property="balance"   column="balance"/>
        <result property="source"    column="source"/>
        <result property="orderId"   column="order_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="PointsResultMap">
        SELECT * FROM points_record WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.shop.entity.PointsRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO points_record
          (user_id, type, points, balance, source, order_id, created_at)
        VALUES
          (#{userId}, #{type}, #{points}, #{balance}, #{source}, #{orderId}, #{createdAt})
    </insert>

    <select id="existsByOrderIdAndType" resultType="boolean">
        SELECT COUNT(1) > 0 FROM points_record
        WHERE order_id = #{orderId} AND type = #{type}
    </select>

    <select id="sumEarned" resultType="int">
        SELECT COALESCE(SUM(points), 0) FROM points_record
        WHERE user_id = #{userId} AND type = 1
    </select>

    <!-- type=2 时 points 为负，取绝对值返回正数 -->
    <select id="sumUsed" resultType="int">
        SELECT COALESCE(-SUM(points), 0) FROM points_record
        WHERE user_id = #{userId} AND type = 2
    </select>

</mapper>
