<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.BrandMapper">

    <resultMap id="BrandResultMap" type="com.shop.entity.Brand">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="logoUrl" column="logo_url"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="BrandResultMap">
        SELECT * FROM brand ORDER BY id ASC
    </select>

    <select id="findByKeyword" resultMap="BrandResultMap">
        SELECT * FROM brand
        <where>
            <if test="keyword != null and keyword != ''">
                name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <select id="findById" resultMap="BrandResultMap">
        SELECT * FROM brand WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.shop.entity.Brand" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO brand (name, logo_url, description, created_at)
        VALUES (#{name}, #{logoUrl}, #{description}, NOW())
    </insert>

    <update id="update" parameterType="com.shop.entity.Brand">
        UPDATE brand
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="logoUrl != null">logo_url = #{logoUrl},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM brand WHERE id = #{id}
    </delete>

</mapper>
